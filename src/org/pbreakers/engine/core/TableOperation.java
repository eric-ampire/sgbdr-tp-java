package org.pbreakers.engine.core;

import javafx.scene.control.Tab;
import org.pbreakers.engine.persistance.Database;
import org.pbreakers.engine.persistance.Table;

import java.io.File;

public class TableOperation {
    public static boolean create(Database database, Table table) {
        // Get database from file
        // Add table to the database
        // Rewrite the file

        return false;
    }

    public static boolean delete(Database database, Table table) {
        // Get database from file
        // delete table to the database
        // Rewrite the file

        return false;
    }

    public static void  updateDatabasee(Database newDatabase, Database old) {

    }
}
